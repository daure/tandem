## Local greeting checks

The `app` checkout contains a Python standard-library command-line application.
From `app`, run `python3 greeting.py Tandem` and expect `Hello, Tandem!` on stdout.
Each invocation loads source from disk. Follow its repository guidance for tests.
