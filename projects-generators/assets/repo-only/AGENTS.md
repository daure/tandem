# Repository workflow

Edit `greeting.py` and run `python3 -m unittest discover -s tests -v` from this directory.
The program and tests use the Python standard library; a successful run reports `OK`.
