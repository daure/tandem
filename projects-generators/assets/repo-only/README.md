# Local greeting

Requires Python 3.11+ with its standard library.

From this repository, run `python3 greeting.py Tandem` to print `Hello, Tandem!`.
Run `python3 -m unittest discover -s tests -v` to verify name handling.
