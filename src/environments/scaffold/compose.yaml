services:
  web:
    image: nginx:1.28-alpine
    volumes:
      - ./site:/usr/share/nginx/html:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/index.html | grep -q 'Tandem template'"]
      interval: 2s
      timeout: 2s
      retries: 30
