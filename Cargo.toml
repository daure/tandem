[package]
name = "tandem"
version = "0.1.5"
edition = "2024"
default-run = "tandem"
description = "A keyboard-first TUI with MCP access"
readme = "README.md"
license = "MIT OR Apache-2.0"
documentation = "https://docs.rs/tandem"
homepage = "https://github.com/daure/tandem"
repository = "https://github.com/daure/tandem"
keywords = ["tui", "terminal", "mcp"]
categories = ["command-line-utilities"]
include = ["Cargo.toml", "Cargo.lock", "README.md", "agent-instructions.md", "workspace-agents.template.md", "migrations/**", "src/**"]

[lib]
name = "tandem"
path = "src/lib.rs"

[[bin]]
name = "tandem"
path = "src/main.rs"

[[test]]
name = "cli-creation"
path = "src/cli/tests/creation.rs"

[dependencies]
axum = "0.8"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive"] }
dirs = "6"
libc = "0.2"
ratatui = "0.30"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls-native-roots"] }
rmcp = { version = "0.12", features = ["server", "macros", "transport-async-rw", "transport-io", "transport-streamable-http-server"] }
rusqlite = { version = "0.37", features = ["bundled"] }
schemars = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = { version = "0.39", default-features = false, features = ["system"] }
tempfile = "3"
tokio = { version = "1", features = ["rt-multi-thread", "io-std", "io-util", "net", "signal", "process"] }
tracing-appender = "0.2"
tuicore = { path = "../tuicore" }

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "off"
strip = "symbols"
